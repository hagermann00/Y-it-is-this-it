{
  "name": "y-it-book-production-system",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "agents:setup": "cd agents && npm install && npm run setup",
    "agents:build": "cd agents && npm run build",
    "agents:start": "cd agents && npm start",
    "agents:cli": "cd agents && npm run cli",
    "agents:api": "cd agents && npm run api",
    "agents:test": "cd agents && npm run test",
    "agents:survey": "cd agents && npm run survey",
    "agents:stats": "cd agents && npm run stats"
  },
  "dependencies": {
    "react-dom": "^19.2.0",
    "react": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
